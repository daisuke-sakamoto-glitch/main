# プロジェクト設定

## 概要
ダウンロードフォルダ整理ツール。ファイルをプロジェクト別・カテゴリ別に自動分類するPythonスクリプト。

## 技術スタック
- Python 3.x
- 標準ライブラリのみ使用（外部依存なし）

## ファイル構成
- `organize_downloads.py` - メインスクリプト

## 開発ルール
- コードのコメントは日本語で書く
- コミットメッセージは日本語で書く
- 既存のコードスタイル（関数名はスネークケース）に合わせる
- 新機能追加時はコマンドライン引数も対応する

## テスト方法
```bash
python organize_downloads.py --dry-run
```

## 注意事項
- Windows / Mac / Linux で動作すること
- ファイル移動時のエラーハンドリングを忘れないこと
